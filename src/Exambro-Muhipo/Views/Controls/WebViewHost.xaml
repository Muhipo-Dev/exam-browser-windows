<UserControl x:Class="ExambroMuhipo.Views.Controls.WebViewHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf">
    <Grid Background="#0B132B">
        <!-- 1. WebView2 Browser Control (Aktif jika WebView2 runtime terpasang) -->
        <wv2:WebView2 x:Name="Wv2BrowserControl" Visibility="Visible" />

        <!-- 2. Windows Native WebBrowser Control (Fallback otomatis untuk Windows 7/8/10 tanpa WebView2) -->
        <WebBrowser x:Name="NativeBrowserControl" Visibility="Collapsed" />

        <!-- Loading / Splash Overlay -->
        <Grid x:Name="LoadingOverlay" Background="#0F172A" Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="6" Background="#1E293B" Foreground="#10B981" BorderThickness="0" Margin="0,0,0,16"/>
                <TextBlock Text="Menyiapkan Lingkungan Ujian Aman..." Foreground="#E2E8F0" FontSize="14" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                <TextBlock x:Name="EngineSubtitle" Text="SMA Muhammadiyah 1 Ponorogo" Foreground="#94A3B8" FontSize="12" Margin="0,4,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
