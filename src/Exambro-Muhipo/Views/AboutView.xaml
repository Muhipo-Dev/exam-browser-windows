<UserControl x:Class="ExambroMuhipo.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="680" d:DesignWidth="1050"
             Background="#0B132B">

    <Grid Margin="32,20">
        <Grid.RowDefinitions>
            <!-- 1. Header Bar with Back Button -->
            <RowDefinition Height="Auto"/>
            <!-- 2. Content -->
            <RowDefinition Height="*"/>
            <!-- 3. Footer -->
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 1. Header Navigation -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Command="{Binding BackToHomeCommand}" 
                    Background="#1E293B" Foreground="#E2E8F0" 
                    Padding="14,7" FontSize="12" FontWeight="Bold" 
                    BorderBrush="#475569" BorderThickness="1" Cursor="Hand">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="7"/>
                    </Style>
                </Button.Resources>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="⬅️ " FontSize="11"/>
                    <TextBlock Text="Kembali ke Beranda"/>
                </StackPanel>
            </Button>

            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="Informasi Aplikasi" FontSize="14" FontWeight="Bold" Foreground="#94A3B8"/>
            </StackPanel>
        </Grid>

        <!-- 2. Main Content Card -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel MaxWidth="800" HorizontalAlignment="Center" Width="800">
                <!-- App Identity Header -->
                <Border Background="#0F172A" CornerRadius="12" Padding="20,16" Margin="0,0,0,14" BorderBrush="#1E293B" BorderThickness="1.5">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <Border Grid.Column="0" Width="52" Height="52" Margin="0,0,16,0" VerticalAlignment="Center">
                            <Image Source="/Assets/SchoolLogo.png" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                        </Border>

                        <StackPanel Grid.Column="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding AppTitle}" FontSize="20" FontWeight="ExtraBold" Foreground="#FFFFFF"/>
                            <TextBlock Text="{Binding AppSubtitle}" FontSize="13" FontWeight="SemiBold" Foreground="#38BDF8" Margin="0,2,0,0"/>
                            <TextBlock Text="{Binding Institution}" FontSize="12" FontWeight="Medium" Foreground="#94A3B8" Margin="0,2,0,0"/>
                        </StackPanel>

                        <Border Grid.Column="2" Background="#1E293B" CornerRadius="8" Padding="12,6" BorderBrush="#334155" BorderThickness="1" VerticalAlignment="Center">
                            <TextBlock Text="{Binding VersionText}" FontSize="11.5" FontWeight="Bold" Foreground="#F59E0B"/>
                        </Border>
                    </Grid>
                </Border>

                <!-- Technical Specs Card -->
                <Border Background="#1E293B" CornerRadius="10" Padding="18,14" Margin="0,0,0,14" BorderBrush="#334155" BorderThickness="1">
                    <StackPanel>
                        <TextBlock Text="⚙️ Spesifikasi &amp; Kompatibilitas Sistem" FontSize="13" FontWeight="Bold" Foreground="#FFFFFF" Margin="0,0,0,8"/>
                        <TextBlock Text="{Binding TechStackInfo}" FontSize="11.5" Foreground="#94A3B8" Margin="0,0,0,6"/>
                        <TextBlock Text="• Engine: Multi-Engine (Microsoft Edge Chromium, Google Chrome &amp; Web Bawaan Windows)" FontSize="11.5" Foreground="#CBD5E1" Margin="0,0,0,3"/>
                        <TextBlock Text="• Dukungan OS: Windows 7 SP1, Windows 8/8.1, Windows 10 &amp; Windows 11 (64-bit)" FontSize="11.5" Foreground="#CBD5E1"/>
                    </StackPanel>
                </Border>

                <!-- Security Statement Card -->
                <Border Background="#182234" CornerRadius="10" Padding="18,14" Margin="0,0,0,10" BorderBrush="#3B82F6" BorderThickness="1">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="🛡️ " FontSize="14"/>
                            <TextBlock Text="Pernyataan Keamanan &amp; Integritas Ujian" FontSize="13" FontWeight="Bold" Foreground="#60A5FA"/>
                        </StackPanel>
                        <TextBlock Text="{Binding SecurityNotice}" FontSize="11.5" Foreground="#CBD5E1" TextWrapping="Wrap" LineHeight="17"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- 3. Footer -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center" Margin="0,8,0,0">
            <TextBlock Text="Developed by Muhipo Dev • Seluruh hak cipta dilindungi" 
                       FontSize="11" Foreground="#64748B" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
