<Window x:Class="ExambroMuhipo.Views.AdminLoginDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Autentikasi Administrator"
        Height="360" Width="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Icon="/Assets/AppIcon.ico">
    
    <Border Background="#0F172A" CornerRadius="16" BorderBrush="#334155" BorderThickness="1.5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" Color="#000000" Opacity="0.6" Direction="270" ShadowDepth="8"/>
        </Border.Effect>

        <Grid Margin="28">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <Border Width="56" Height="56" Background="#1E293B" CornerRadius="14" HorizontalAlignment="Center" Margin="0,0,0,12" BorderBrush="#3B82F6" BorderThickness="1.5" Padding="4">
                    <Image Source="/Assets/SchoolLogo.png" Stretch="Uniform" RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border>
                <TextBlock x:Name="DialogTitleText" Text="Autentikasi Administrator" Foreground="#F8FAFC" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                <TextBlock x:Name="DialogSubtitleText" Text="SMA Muhammadiyah 1 Ponorogo" Foreground="#94A3B8" FontSize="12" Margin="0,4,0,0" HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Description -->
            <TextBlock Grid.Row="1" x:Name="DescriptionText" 
                       Text="Masukkan password administrator untuk melanjutkan tindakan ini." 
                       Foreground="#CBD5E1" FontSize="13" TextWrapping="Wrap" TextAlignment="Center" Margin="0,0,0,16"/>

            <!-- Password Input -->
            <StackPanel Grid.Row="2" VerticalAlignment="Center">
                <TextBlock Text="Password Administrator" Foreground="#94A3B8" FontSize="12" FontWeight="SemiBold" Margin="0,0,0,6"/>
                <Border Background="#1E293B" CornerRadius="8" BorderBrush="#475569" BorderThickness="1" Padding="4">
                    <PasswordBox x:Name="PasswordInput" 
                                 Background="Transparent" 
                                 Foreground="#FFFFFF" 
                                 CaretBrush="#38BDF8"
                                 FontSize="15" 
                                 BorderThickness="0" 
                                 Padding="8,6"
                                 PreviewKeyDown="PasswordInput_PreviewKeyDown"
                                 KeyDown="PasswordInput_KeyDown"/>
                </Border>
                <TextBlock x:Name="ErrorText" Text="Password salah. Silakan coba kembali." 
                           Foreground="#EF4444" FontSize="12" FontWeight="SemiBold" 
                           Visibility="Collapsed" Margin="0,8,0,0"/>
            </StackPanel>

            <!-- Action Buttons -->
            <Grid Grid.Row="3" Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Content="Batal" Height="40" Background="#334155" Foreground="#E2E8F0" FontWeight="SemiBold" FontSize="13" BorderThickness="0" Cursor="Hand" Click="CancelButton_Click" IsCancel="True">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Grid.Column="2" Content="Konfirmasi" Height="40" Background="#2563EB" Foreground="#FFFFFF" FontWeight="Bold" FontSize="13" BorderThickness="0" Cursor="Hand" Click="ConfirmButton_Click" IsDefault="True">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </Grid>
        </Grid>
    </Border>
</Window>
